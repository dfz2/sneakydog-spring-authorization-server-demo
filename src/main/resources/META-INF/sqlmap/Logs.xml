<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dog.sneaky.demo.database.dao.LogsDAO">

    <select id="loadLoginedByUsername" parameterType="String" resultType="LogsDO">
        select * from logs where username = #{username} order by createdAt desc limit 1
    </select>

    <select id="listLogsByUsername" parameterType="String" resultType="LogsDO">
        select * from logs where username = #{username} order by createdAt desc limit 10
    </select>
    <insert id="insert" parameterType="LogsDO">
        insert into logs(username, ipAddress, region, userAgent, createdAt, loginStatus)
        values (#{username}, #{ipAddress}, #{region}, #{userAgent}, #{createdAt} , #{loginStatus})
    </insert>



</mapper>
