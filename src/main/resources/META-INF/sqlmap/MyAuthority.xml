<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dog.sneaky.demo.database.dao.MyAuthorityDAO">

    <select id="loadPermsByUsername" parameterType="String" resultType="String">
        select menus.perms from users
        inner join user_role on users.id = user_role.userId
        inner join role_menus on user_role.roleId = role_menus.roleId
        inner join menus on menus.id = role_menus.menuId
        where menus.perms is not null and users.username = #{username}
    </select>

</mapper>
